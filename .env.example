# Copy to .env and fill in.

# --- Claude (scoring + memo synthesis) ---
# If unset, the pipeline falls back to a deterministic MOCK reasoning engine
# so it still produces a PDF without an API key.
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-opus-4-8

# --- Langflow REST API (only needed when running through the flow/UI) ---
LANGFLOW_URL=http://localhost:7860
LANGFLOW_FLOW_ID=credit-analyst
# LANGFLOW_API_KEY=    # if your Langflow instance requires auth

# --- Paths (defaults are fine for local dev) ---
INPUTS_DIR=inputs
OUTPUTS_DIR=outputs
